function comprar() {

    cantidad = cantidad + 1;
    // Imprimimos la compra 
    console.log(cantidad);
    // Le avisamos la cantidad comprada 
    alert("Compraste" + cantidad + "productos")
}

function carrito() {
    totalPagar = cantidad * precio 

    conosle.log (totalPagar);

    resultado = pago - totalPagar
}